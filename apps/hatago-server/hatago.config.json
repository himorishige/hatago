{
  "proxy": {
    "servers": [
      {
        "endpoint": "https://mcp.deepwiki.com/mcp",
        "id": "deepwiki",
        "timeout": 30000
      }
    ],
    "namespaceStrategy": "prefix",
    "conflictResolution": "rename"
  }
}
