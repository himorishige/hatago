# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Publishing Configuration
access=public

# Package lock settings  
# package-lock=false  # This disables npm's package-lock.json (not needed for pnpm)
prefer-frozen-lockfile=true

# Authentication for publishing (CI/CD only)
# The NPM_TOKEN environment variable should be set in GitHub Secrets
# Note: This line is only used in CI/CD environments where NPM_TOKEN is set