#
{{titleCase name}}
Plugin

{{description}}

## Installation This plugin is part of your Hatago project. Make sure you have the required
dependencies: ```bash pnpm install @hatago/core ``` ## Usage Register the plugin in your Hatago
server: ```typescript import {
{{camelCase name}}Plugin } from './plugins/{{kebabCase name}}.js' import { createHatagoApp } from
'@hatago/core' const { app, server } = createHatagoApp({ name: 'my-server', plugins: [
{{camelCase name}}Plugin, ], }) ```

{{#if hasTools}}
  ## Available Tools

  {{#each tools}}
    ###
    {{name}}

    {{description}}

    **Usage:** ```bash # Via MCP client { "jsonrpc": "2.0", "id": 1, "method": "tools/call",
    "params": { "name": "{{name}}", "arguments": { // Add your parameters here } } } ```

  {{/each}}
{{/if}}

{{#if hasResources}}
  ## Available Resources

  {{#each resources}}
    ###
    {{name}}

    {{description}}

    **URI:** `{{uri}}` **MIME Type:** `{{mimeType}}`

  {{/each}}
{{/if}}

{{#if hasHttpRoutes}}
  ## HTTP Endpoints

  {{#each routes}}
    ###
    {{toUpperCase method}}
    {{path}}

    Available at: `{{method}}
    {{path}}`

  {{/each}}
{{/if}}

{{#if hasConfiguration}}
  ## Configuration The following environment variables can be set:

  {{#each configOptions}}
    - `{{envVar}}` -
    {{#if required}}**Required.** {{/if}}{{#if defaultValue}}Default: `{{defaultValue}}`{{/if}}
  {{/each}}

{{/if}}

## Development To modify this plugin: 1. Edit the plugin logic in `{{kebabCase name}}.ts` 2. Update
tests in `{{kebabCase name}}.test.ts` 3. Restart your development server: `pnpm dev` ## Testing
```bash # Run plugin tests pnpm test
{{kebabCase name}}

# Test via curl curl -X POST http://localhost:8787/mcp \\ -H "Content-Type: application/json" \\ -d
'{ "jsonrpc": "2.0", "id": 1, "method": "tools/list" }' ``` --- *Generated by Hatago CLI on
{{timestamp}}*